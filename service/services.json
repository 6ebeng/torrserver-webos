{
	"id": "com.torrserver.app.service",
	"description": "TorrServer control service - manages the TorrServer process",
	"services": [
		{
			"name": "com.torrserver.app.service",
			"description": "Downloads, starts, stops and monitors the TorrServer server",
			"commands": [
				{ "name": "status", "description": "Return current TorrServer status" },
				{ "name": "start", "description": "Start TorrServer (installs the bundled binary on first run)" },
				{ "name": "stop", "description": "Stop TorrServer" },
				{ "name": "restart", "description": "Restart TorrServer" },
				{ "name": "install", "description": "Reinstall the bundled TorrServer binary" },
				{ "name": "getLogs", "description": "Return the tail of the TorrServer log" },
				{ "name": "enableAutostart", "description": "Enable launch at boot" },
				{ "name": "disableAutostart", "description": "Disable launch at boot" },
				{ "name": "autostart", "description": "Boot hook entry point (starts TorrServer)" },
				{ "name": "launchLampa", "description": "Launch the Lampa app if installed" },
				{ "name": "launchMediaPlayer", "description": "Launch the TV media player (Photo/Video or MediaPlayer)" },
				{ "name": "getDeviceInfo", "description": "Return the TV firmware and webOS version" },
				{ "name": "listStorage", "description": "List USB storage for the torrent cache and the current cache path" },
				{ "name": "setStorage", "description": "Set the torrent cache/downloads location (USB path or internal RAM)" },
				{ "name": "setAuth", "description": "Set the web UI / API Basic Auth username and password" },
				{ "name": "resetAuth", "description": "Restore the default web UI / API credentials" }
			]
		}
	]
}
